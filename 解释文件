0.使用超像素进行分类计算
    超像素的数据
1.目前最好的结果的过程:
    使用image和rim的差值作为目标patch
    训练数据生成是筛选60万个数据
    训练数据预先分成15类，并使用pca进行回归分类
    *回归分类不使用动态分类，而是只进行分类调整，迭代20次
    *测试数据从 0 0 开始采集
    patch组合的时候使用投票（16近邻），没有用到误差
    得到的baby为 psnr：35.26(16近邻)  35.24（4近邻） 34.48(patch重叠为1)
    位置：[3:507,3:507]

10.稀疏编码的版本，由于系数编码需要均值为0 方差为1
    直接使用image减去均值除以对应特征方差作为目标数据，特征数据减去均值除以方差作为输入数据
    训练数据生成是筛选60万个数据
    训练数据预先分成15类，并使用pca进行回归分类
    回归分类使用动态分类，迭代100次
    *测试数据从 0 0 开始采集
    * 在回归的过程中筛选了每组基，保证每一个atom的均方根>0.7
    patch组合的时候使用投标，没有用到误差
    得到的baby为 psnr：34.7

